apiVersion: v1
kind: Service
metadata:
  name: calculator
spec:
  type: ClusterIP
  selector:
    app: calculator
  ports:
    - name: "8080"
      port: 8080
      protocol: TCP
      targetPort: 8080
